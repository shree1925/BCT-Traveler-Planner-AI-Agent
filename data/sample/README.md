# Sample data

Tiny synthetic CSVs that mirror the **real Kaggle column headers**, so the app
runs before you add your own files.

    cp data/sample/*.csv data/cleaned/

Replace them with your six cleaned CSVs when ready. The filenames the app looks
for are listed in `data_layer/schema.py`.
